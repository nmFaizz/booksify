

const BooksCollection = ({
    children,
    pageName,
}: {
    children: React.ReactNode,
    pageName: string
}) => {
    return (
        <div>
                
        </div>
    )
}

export default BooksCollection